# Task Manager Configuration

# Data directory (optional, defaults to ~/.taskmanager)
# TASKMANAGER_DATA_DIR=/path/to/data

# Linear API Configuration (for sync feature)
# LINEAR_API_KEY=lin_api_xxxxxxxxxxxxx
# LINEAR_TEAM_ID=TEAM-XXX

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO